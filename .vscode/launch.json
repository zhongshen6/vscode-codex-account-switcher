{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Codex Account Switcher",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}"
      ]
    }
  ]
}
